import React from "react";
import {screen, render} from '@testing-library/react';

const startClock = () => {
    let miliseconds = new Date().getTime();
    console.log(`Start time ${clock}`);
  };

test('Create a new miliseconds variable ', ()=> {
    expect(Number);
})